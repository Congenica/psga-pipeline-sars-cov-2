---
- hosts: all
  become: false
  vars:
    docker_build:
      path: "/home/vagrant/psga"
    git_submod:
      path: "/home/vagrant/psga"
    - { role: ../../install-os-packages, yum_update: false, become: yes }
    - { role: ../../build-auspice }
