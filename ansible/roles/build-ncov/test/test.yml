---
- hosts: all
  become: false
  vars:
    docker_build:
      path: '~/psga'
    git_submod:
      path: '~/psga'
  roles:
    - { role: ../../install-os-packages, yum_update: false, become: yes }
    - { role: ../../build-ncov }
