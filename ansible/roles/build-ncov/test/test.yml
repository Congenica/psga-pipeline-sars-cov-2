---
- hosts: all
  become: false
  vars:
    docker_build:
      path: '~/covid-pipeline'
    git_submod:
      path: '~/covid-pipeline'
  roles:
    - { role: ../../install-os-packages, yum_update: false, become: yes }
    - { role: ../../build-ncov }
