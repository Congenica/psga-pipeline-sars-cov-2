params {
    /*
    This has to be here and included in nextflow.config to make ncov run. I can't explain why.
    It doesn't care which param is set within it. It just needs to exist with a param.
    I've left two in because I don't understand.
    */
    help = false
    print_config = false
}